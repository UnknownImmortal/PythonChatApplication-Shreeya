# PythonChatApplication-Shreeya
A two-way Python chat app using socket programming (TCP) with a Tkinter GUI. One instance runs as server, another as client — both send and receive messages live in real-time chat windows. Built with server.py, client.py, layout.py, and main.py for clean separation of networking and interface logic.
